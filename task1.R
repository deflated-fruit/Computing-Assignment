# Simon Toogood
# Task 1

# Define distance along the screen, from -y.max to +y.max
y.max <- 200    # Size of the screen
n.screen <- 501 # Sets the number of pixels on the screen (ie. the resolution)
screen <- seq(-y.max, y.max, length.out=n.screen)

print(screen)
